const Header = require('./common/header.component');
const SideBar = require('./common/sidebar.component');
const Footer = require('./common/footer.component');

const Form = require('./login/form.component');
const Credentials = require('./login/credentials.component');

const InventoryHeader = require('./inventory/inventory-header.component');

module.exports = {
  Header,
  SideBar,
  Footer,
  Form,
  Credentials,
  InventoryHeader,
};
